{% extends 'base.html.twig' %}

{% block title %}Edit Bead
{% endblock %}

{% block body %}
	<h1>Edit Bead</h1>

	<h3>
		Create new Project</h3>
	{{ form_start(projectForm) }}
	{{ form_errors(projectForm) }}

	<div class="container">
		<div
			class="card" style="width: 30rem;">
			{# <img src="..." class="card-img-top" alt="..."> #}

			<div class="card-body">
				<p class="card-text">{{ form_row(projectForm.name) }}</p>
			</div>
			<div class="card-body">
				<p class="card-text">{{ form_row(projectForm.description) }}</p>
			</div>
			<div class="card-body">
				<button class="btn btn-primary">Save</button>
			</div>
		</div>
		{{ form_end(projectForm) }}


	</div>
	<script>
		document.addEventListener('DOMContentLoaded', function () {

$('.select2').select2({placeholder: "Choose an option", allowClear: true});
});
	</script>
{% endblock %}
